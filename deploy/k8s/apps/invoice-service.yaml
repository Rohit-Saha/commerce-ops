apiVersion: apps/v1
kind: Deployment
metadata:
  name: invoice-service
  namespace: commerce-ops
  labels:
    app.kubernetes.io/name: invoice-service
    app.kubernetes.io/part-of: commerce-ops
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: invoice-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: invoice-service
        app.kubernetes.io/part-of: commerce-ops
    spec:
      containers:
      - name: invoice-service
        image: commerceops/invoice-service:0.1.0-SNAPSHOT
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8089
        envFrom:
        - configMapRef:
            name: commerce-ops-config
        - secretRef:
            name: commerce-ops-secrets
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: http
          initialDelaySeconds: 20
          periodSeconds: 10
          failureThreshold: 6
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: http
          initialDelaySeconds: 40
          periodSeconds: 20
          failureThreshold: 3
        resources:
          requests:
            cpu: 100m
            memory: 384Mi
          limits:
            cpu: "1"
            memory: 768Mi
---
apiVersion: v1
kind: Service
metadata:
  name: invoice-service
  namespace: commerce-ops
  labels:
    app.kubernetes.io/name: invoice-service
    app.kubernetes.io/part-of: commerce-ops
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: invoice-service
  ports:
  - name: http
    port: 8089
    targetPort: http
