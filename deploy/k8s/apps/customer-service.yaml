apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service
  namespace: commerce-ops
  labels:
    app.kubernetes.io/name: customer-service
    app.kubernetes.io/part-of: commerce-ops
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: customer-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: customer-service
        app.kubernetes.io/part-of: commerce-ops
    spec:
      containers:
      - name: customer-service
        image: commerceops/customer-service:0.1.0-SNAPSHOT
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8087
        envFrom:
        - configMapRef:
            name: commerce-ops-config
        - secretRef:
            name: commerce-ops-secrets
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: http
          initialDelaySeconds: 20
          periodSeconds: 10
          failureThreshold: 6
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: http
          initialDelaySeconds: 40
          periodSeconds: 20
          failureThreshold: 3
        resources:
          requests:
            cpu: 100m
            memory: 384Mi
          limits:
            cpu: "1"
            memory: 768Mi
---
apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: commerce-ops
  labels:
    app.kubernetes.io/name: customer-service
    app.kubernetes.io/part-of: commerce-ops
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: customer-service
  ports:
  - name: http
    port: 8087
    targetPort: http
